### Connect to engine node
via SSH Gate

### Create remote docker-machine connection
reimport SSH keys in the addon menu

### Connect the engine to a swarm cluster
```
docker swarm join --token $TOKEN $HOST:$PORT
```
